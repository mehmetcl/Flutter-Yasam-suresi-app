import 'package:flutter/material.dart';

class UserData {
  String? genderSelection;
  double smoking;
  double sportsDay;
  int userlength;
  int kilogram;

  UserData(this.genderSelection, this.smoking, this.sportsDay, this.kilogram,this.userlength);
}
